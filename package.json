{
  "name": "spell-trail",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "prebuild": "node scripts/sync-trail-art.mjs",
    "dev": "vite",
    "build": "vite build",
    "test": "vitest run",
    "preview": "vite preview",
    "deploy": "npm run build && wrangler deploy -c wrangler.prod.jsonc"
  },
  "dependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "vite": "^6.1.0"
  },
  "devDependencies": {
    "vitest": "^3.2.4",
    "wrangler": "^4.123.0"
  }
}
